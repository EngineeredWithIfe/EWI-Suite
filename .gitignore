# The EWI Suite bundle IS published so GitHub Pages can serve real downloads to
# every visitor. Ignore stray zips elsewhere, but keep the published bundle.
*.zip
!downloads/
!downloads/ewi-suite.zip

# OS / editor noise
.DS_Store
